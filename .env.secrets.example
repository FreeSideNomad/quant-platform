# Copy to .env.secrets (gitignored) and fill in.
# For the dev VM deployment, this file lives at /srv/quant-platform/.env.secrets on the VM and is NOT committed.

QUANT_DB_PASSWORD=replace-with-password-from-team-1password
SESSION_JWT_SIGNING_KEY=generate-with-openssl-rand-hex-32
