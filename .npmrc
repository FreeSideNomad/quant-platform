# Force optional deps to install even when the host platform differs from the
# lockfile's host — fixes rolldown/esbuild native binding errors in Linux CI
# when the lockfile was last updated on macOS (and vice versa).
package-manager-strict=false
auto-install-peers=true
node-linker=hoisted
