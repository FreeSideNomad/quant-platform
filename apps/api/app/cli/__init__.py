"""Operator CLI surface for the platform.

Subcommands are exposed as separate Typer apps and composed in
the entry-point script. For local use, invoke as:
    uv run python -m app.cli.audit_verify
"""
